@model IEnumerable<LTA.Models.DonThanhToan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoPhieuThanhToan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaDanhMuc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TienDanhMuc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoDon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.SoPhieuThanhToan)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MaDanhMuc)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TienDanhMuc)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.SoDon)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.SoPhieuThanhToan }) |
        @Html.ActionLink("Details", "Details", new { id = item.SoPhieuThanhToan }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.SoPhieuThanhToan })
    </td>
</tr>
}

</table>
