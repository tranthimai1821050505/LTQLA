@model IEnumerable<LTA.Models.QuanBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaQuan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenQuan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.MaQuan)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TenQuan)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.MaQuan }) |
        @Html.ActionLink("Details", "Details", new { id = item.MaQuan }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.MaQuan })
    </td>
</tr>
}

</table>
