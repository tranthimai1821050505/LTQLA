@model IEnumerable<LTA.Models.Ca>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoCa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ngay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.SoCa)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Ngay)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.SoCa }) |
        @Html.ActionLink("Details", "Details", new { id = item.SoCa }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.SoCa })
    </td>
</tr>
}

</table>
