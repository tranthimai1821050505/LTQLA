@model LTA.Models.CheckAccount

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Login</h2>

    @using (Html.BeginForm("Login", "CheckAccount", new { ReturnURL = ViewBag.returnUrl }))
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4>CheckAccount</h4>
            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                @Html.LabelFor(model => model.CheckUsername, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.CheckUsername, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.CheckUsername, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                @Html.LabelFor(model => model.CheckPassword, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.CheckPassword, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.CheckPassword, "", new { @class = "text-danger" })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Login" class="btn btn-default" />
                </div>
            </div>
        </div>
    }

@*<form action="@Url.Action("Login","CheckAccount")" method="post">
    <label>CheckUsername</label>
    <input type="text" name="CheckUsername" value="@ViewBag.CheckUsername" />
    <span style="color: red">@ViewBag.usernameError</span>
    <label>CheckPassword</label>
    <input type="password" name="CheckPassword" value="" placeholder="**********" />
    <span style="color: red">@ViewBag.passwordError</span>
    <button type="submit">Sign in</button>
    <button type="reset">Clear</button>
</form>


@if (ViewBag.invalidData != null)
{
    <script>
        alert("@ViewBag.invalidData");
    </script>
}*@

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
