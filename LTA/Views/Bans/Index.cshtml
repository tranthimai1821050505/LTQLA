@model IEnumerable<LTA.Models.Ban>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoBan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.SoBan)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.SoBan }) |
        @Html.ActionLink("Details", "Details", new { id = item.SoBan }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.SoBan })
    </td>
</tr>
}

</table>
